About Syncterm Builds
=====================

By default the build script will install in $HOME/syncterm but I prefer to
copy those files to my $HOME so the installdir is just for keeping the files
together.
